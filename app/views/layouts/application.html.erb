<!DOCTYPE html>
<html>
<head>
  <title>Tuteable :: <%= content_for :title%></title>
  <%= stylesheet_link_tag    "application", :media => "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

  <div class="row top-bar">
    <div class="col-md-4 top-bar-col top-bar-1"></div>
    <div class="col-md-4 top-bar-col top-bar-2"></div>
    <div class="col-md-4 top-bar-col top-bar-3"></div>
  </div>

  <div class="container">

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div class="header">
      <ul class="nav nav-pills pull-right">
        <li>
          <p class="navbar-text pull-right">
            <% if admin_signed_in? %>
              Logged in as <strong><%= current_admin.email %></strong>.
              <%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %> |
              <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
            <% else %>
              <%= link_to "Sign up", new_admin_registration_path, :class => 'navbar-link'  %> |
              <%= link_to "Login", new_admin_session_path, :class => 'navbar-link'  %>
            <% end %>
          </p>
        </li>
      </ul>
      <h3 class="text-muted">Tute<span class="header-h3-tuteable-2">able</span></h3>
    </div>

    <div class="requests-container">
      <div class="requests-container-header">
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle requests-link" data-toggle="dropdown">
            Request
            <span class="caret"></span>
          </button>
          <ul class="dropdown-menu">
            <li><%= link_to "Requests", requests_path, class: "requests-link" %></li>
            <li><%= link_to "New Request", new_request_path, class: "requests-link" %></li>
          </ul>
        </div>
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle requests-link" data-toggle="dropdown">
            Scheduled
          </button>
        </div>
        <div class="btn-group">
          <button type="button" class="btn btn-default dropdown-toggle requests-link" data-toggle="dropdown">
            History
          </button>
        </div>
        <input type="text" >
      </div>

      <%= yield %>
    </div>

  </div>

  <div class="footer-menu">
    <div class="container">
      <div class="row">
        <div class="col-md-2">About Us</div>
        <div class="col-md-2">How It Works</div>
        <div class="col-md-2">Contact</div>
        <div class="col-md-2">Privacy</div>
        <div class="col-md-2">Terms</div>
        <div class="col-md-2">Support</div>
      </div>
    </div>
  </div>

  <div class="footer-bottom">
    <p class="footer-bottom-title">Tuteable</p>
    <p class="footer-bottom-copyright">COPYRIGHT @ 2014 TUTEABLE. ALL RIGHTS RESERVED</p>
  </div>

</body>
</html>
